name: pdks_mobile
description: "PDKS - Personnel Attendance Tracking System"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.0.0
  flutter_riverpod: ^2.4.0
  go_router: ^14.0.0
  firebase_messaging: ^15.0.0
  firebase_core: ^3.0.0
  intl: ^0.20.2
  shared_preferences: ^2.2.0
  # table_calendar removed — custom calendar grid used instead
  flutter_dotenv: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
